{
  "name": "horizon-redux-sync",
  "version": "1.0.0",
  "description": "Keeps RethinkDB collections in sync with Redux sub-trees, using Horizon as the transport",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --presets babel-preset-es2015 --out-dir dist",
    "prepublish": "npm run build"
  },
  "keywords": [
    "encore",
    "rethinkdb",
    "horizon",
    "redux"
  ],
  "author": "Stian Håklev <shaklev@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-preset-es2015": "^6.6.0"
  },
  "dependencies": {
    "deep-equal": "^1.0.1",
    "json-path": "^0.1.3"
  }
}
